server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: ${JWT_SECRET:sinker26-dev-secret-key-change-in-production-min-32-bytes!}
    expiration-ms: 86400000
  # 外部整合：PDCA recompute、ERP 採購單（URL 與憑證請以環境變數或本機設定覆寫）
  integrations:
    auth:
      token-url: ${ERP_TOKEN_URL:}
      account: ${ERP_ACCOUNT:}
      salasana: ${ERP_SALASANA:}
    pdca:
      enabled: ${PDCA_RECOMPUTE_ENABLED:false}
      recompute-url: ${PDCA_RECOMPUTE_URL:}
    erp:
      enabled: ${ERP_PURCHASE_ORDER_ENABLED:false}
      purchase-order-url: ${ERP_PURCHASE_ORDER_URL:}
    erp-product:
      enabled: ${ERP_PRODUCT_ENABLED:false}
      product-list-url: ${ERP_PRODUCT_LIST_URL:}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
